import { GlobalConfigs } from "@/types/global";

const globalConfigs: GlobalConfigs = {
  main: {
    title: "Admin Panel",
  },
  sidebar: {
    collapsible: "icon",
  },
};

export default globalConfigs;
